export interface GeneratorData {
    id: string;
    Waste_Type: string;
    userId: string; // Ensure userId is part of the structure
    Description?: string; // Optional fields (add more if needed)
    Address?: string;
  }
  